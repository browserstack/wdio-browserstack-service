---
"@wdio/browserstack-service": minor
---

- App Accessibility scans triggered inside test hooks (`before`/`after`, `beforeEach`/`afterEach`) are now correctly attributed to the wrapping test instead of being dropped.
