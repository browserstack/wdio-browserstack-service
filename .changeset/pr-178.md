---
"@wdio/browserstack-service": patch
---

- Fixed skipped tests staying stuck on "In Progress" in Test Hub — and their build later being marked as timed out — when a spec ends with skipped tests, such as `describe.skip()` or a trailing `it.skip()`.
