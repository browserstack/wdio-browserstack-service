---
"@wdio/browserstack-service": minor
---

- Tests that never run because Mocha's `bail` halted a spec are now reported as `skipped` instead of being left out of the report — so a Test Run accounts for every test in that spec, including tests in sibling `describe` blocks.
