---
"@wdio/browserstack-service": minor
---

- Tests that never run because of `bail` are now reported as `skipped` instead of being left out of the report — so a Test Run accounts for every test it was meant to execute.
- Covers both `mochaOpts.bail` (the rest of the failing spec) and WebdriverIO's `bail` (spec files that never started).
