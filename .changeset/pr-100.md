---
"@wdio/browserstack-service": patch
---

- Fixed App Automate session name and status (and Percy screenshot routing) not being applied when the app is provided via an `appium:app`-style capability instead of the top-level `app` option — the requests were routed to the web Automate endpoint and silently no-op'd.
