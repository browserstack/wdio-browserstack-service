---
"@wdio/browserstack-service": patch
---

- Fixed accessibility scanning stopping for the rest of a test after `browser.reloadSession()`.
- The hooks defined in your WDIO config are now included in the debug logs, so support can see the code behind a failure.
