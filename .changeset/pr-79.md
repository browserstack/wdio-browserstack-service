---
"@wdio/browserstack-service": patch
---

- Fixed test cases being dropped from the dashboard when their `before all`/`before each`/`after each` hook fails — they now correctly appear as Skipped, and the failing hook (and the build) are correctly reported as failed.
